<% content_for :page_title, "Nuevo Repuesto / Insumo" %>

<div class="card shadow-sm border-0 max-w-700 mx-auto">
  <div class="card-body p-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h4 class="card-title mb-0 fw-bold text-dark">Registrar en Bodega</h4>
      <%= link_to "Volver", warehouse_items_path, class: "btn btn-outline-secondary rounded-pill px-3" %>
    </div>

    <%= render "form" %>
  </div>
</div>
