<% content_for :page_title, "Editar Cliente" %>

<div class="card shadow-sm border-0 max-w-600 mx-auto">
  <div class="card-body p-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h4 class="card-title mb-0 fw-bold text-dark">Editar Cliente</h4>
      <%= link_to "Volver", clients_path, class: "btn btn-outline-secondary rounded-pill px-3" %>
    </div>

    <%= render "form" %>
  </div>
</div>
